//In this program we will see the use of "var" as the local variable

namespace UseOfVar
{
    class MainClass
    {
        static void Main()
        {
            var IntegerType = 10;
            var StringType = "Hello";
            System.Console.WriteLine("INTEGER USING VAR : " + IntegerType);
            System.Console.WriteLine("STRING USING VAR : " + StringType);
        }
    }
}
